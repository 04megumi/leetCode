/*
    题目要求: O(log(m+n))

    初步想法:
    (1) 二分
    (2) ==> (m1+m2)=(m+n)/2, max(nums[m1],nums[m2])<min(nums[m1+1],nums[m2+2])
    (3) 只需找到一种二分方法 快速找到(2)中的解

*/

class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        int 
    }
};
